# seowon-blog-assets

세무회계 서원 네이버 블로그 카드뉴스 이미지 저장소. 파이프라인(`TaxHub/taxhub-blog-pipeline`, `src/publish_assets.py`)이 글마다 `posts/<YYYYMMDD-해시>/images/` 로 push 한다.
네이버 에디터가 https 주소 이미지를 붙여넣기에 받아주기 때문에(2026-09-09 실측) 공개로 둔다. 블로그에 공개되는 이미지만 있다.
